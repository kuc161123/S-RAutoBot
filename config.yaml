bybit:
  alt_base_url: 'https://api.bytick.com'
  api_key: '${BYBIT_API_KEY}'
  api_secret: '${BYBIT_API_SECRET}'
  base_url: 'https://api.bybit.com'
  use_alt_on_fail: true
  ws_public: 'wss://stream.bybit.com/v5/public/linear'
  ws_public_alt: 'wss://stream.bytick.com/v5/public/linear'

execution:
  enabled: true
  entry_slippage_pct: 0.0003
  fee_pct: 0.0006

indicators:
  atr_period: 14
  ema_periods: [200]  # 200 EMA for 1H timeframe
  rsi_period: 14

legacy:
  partial_take_profit: false
  scalp_mode: false
  trailing_stop: false
  volume_filter: false
  vwap_filter: false

monitoring:
  health_check: true
  heartbeat_interval_sec: 3600

notifications:
  daily_summary: true
  entry: true
  exit: true
  pending_signals: true
  startup: true

risk:
  max_concurrent_positions: 5  # Conservative for 15 symbols
  max_daily_loss: 0.05
  max_position_size_pct: 0.1
  risk_per_trade: 0.005

strategy:
  entry_params:
    max_wait_candles: 6
    require_bos: true
  exit_params:
    sl_atr_mult: 1.0
  name: 1h_trend_divergence_robust  # Validated anti-overfit strategy
  signal_params:
    lookback_bars: 10
    min_pivot_distance: 3
    pivot_right: 3
    rsi_period: 14
  timeframe: '60'  # 1H timeframe
  trend_filter:
    daily_ema_proxy: 200  # 200 EMA for 1H
    enabled: true

# 15 ROBUST Symbols - Passed ALL Anti-Overfit Tests
# OOS/Train Ratio verified, Monte Carlo positive, Stress Test passed
symbols:
  # Top Performers (OOS >= +15R)
  SUIUSDT: {enabled: true, rr: 8.0}     # OOS: +28.9R ⭐
  AAVEUSDT: {enabled: true, rr: 7.0}    # OOS: +21.5R ⭐
  KAITOUSDT: {enabled: true, rr: 7.0}   # OOS: +20.0R ⭐
  ICPUSDT: {enabled: true, rr: 6.0}     # OOS: +18.3R ⭐
  ENAUSDT: {enabled: true, rr: 8.0}     # OOS: +18.0R ⭐
  
  # Strong Performers (OOS +10-15R)
  PENGUUSDT: {enabled: true, rr: 8.0}   # OOS: +13.9R
  HBARUSDT: {enabled: true, rr: 4.5}    # OOS: +12.9R
  STORJUSDT: {enabled: true, rr: 6.0}   # OOS: +12.1R
  AVAXUSDT: {enabled: true, rr: 5.5}    # OOS: +10.3R
  
  # Solid Performers (OOS +1-10R)
  SOLUSDT: {enabled: true, rr: 7.0}     # OOS: +7.1R
  FARTCOINUSDT: {enabled: true, rr: 7.0} # OOS: +5.4R
  AIXBTUSDT: {enabled: true, rr: 6.0}   # OOS: +4.3R
  HIVEUSDT: {enabled: true, rr: 8.0}    # OOS: +3.1R
  WLDUSDT: {enabled: true, rr: 7.0}     # OOS: +2.8R
  VIRTUALUSDT: {enabled: true, rr: 5.5} # OOS: +0.9R

telegram:
  chat_id: ${TELEGRAM_CHAT_ID}
  poll_mode: legacy
  probe:
    get_updates_once: true
    limit: 3
  token: ${TELEGRAM_BOT_TOKEN}
